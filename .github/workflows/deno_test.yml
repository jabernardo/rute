name: Deno Test

on:
  push:
    branches: [ 0.x ]
  pull_request:
    branches: [ 0.x ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@master
      - uses: denolib/setup-deno@master
        with:
          deno-version: 0.39.0
      - run: deno test

